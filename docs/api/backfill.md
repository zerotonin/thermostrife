# thermostrife.backfill

CLI entry point: fill `day_temp_C` and the period-correct decadal mean
for every row, with provenance tracking.

```{automodule} thermostrife.backfill
:members:
:undoc-members:
```
